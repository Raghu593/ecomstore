import { createSlice } from "@reduxjs/toolkit";

const initialState={
        // loading,
        // userInfo,
        // error
}

const userSlice=createSlice({
    name:'user',
    initialState,
    reducers:{  
            login:()=>{
                    
            }
    }
})

export default userSlice.reducer;